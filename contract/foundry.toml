[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.19"
optimizer = true
optimizer_runs = 200
gas_reports = ["*"]

[rpc_endpoints]
linea = "${LINEA_RPC_URL}"
linea_testnet = "${LINEA_TESTNET_RPC_URL}"

[etherscan]
linea = { key = "${ETHERSCAN_API_KEY}" }
linea_testnet = { key = "${ETHERSCAN_API_KEY}" }